# =============================================================
# GlobeTrotter — Frontend Environment Variables
# Copy to .env and fill in values.
# NEVER commit .env
# =============================================================

# Backend API URL
VITE_API_URL=http://localhost:5000/api

# App name (shown in browser title etc.)
VITE_APP_NAME=GlobeTrotter
